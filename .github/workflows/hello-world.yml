name: Hello World

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print hello world
        run: echo "Hello, world!"

    
